---
import Button from '@components/Button.astro';
import DateInfoHome from '@components/DateInfoHome.astro';
import Layout from '@layouts/Layout.astro';
---

<Layout title="JSConf Chile 2024">
  <main>
    <section class="hero">
      <div class="hero-info">
        <img src="/assets/JSCONF.svg" alt="" />
        <p>
          Acompáñanos, en la primera edición Chilena, de la más prestigiosa conferencia de
          JavaScript. 2 días de Charlistas internacionales, comunidad, aprendizaje y conexiones,
          este 5, 6 y 7 de diciembre, 2024
        </p>
        <div class="info-buttons">
          <Button color="white" link="#" icon="star"> Sponsor </Button>
          <Button variant="stroke" color="white" link="#" icon="ticket"> Tickets </Button>
        </div>
      </div>
      <div class="hero-date">
        <DateInfoHome left="Place" right={'("Santiago");'} filled position="first" />
        <DateInfoHome left="Date" right={'(“Feb 00 2024”);'} filled position="second" />
        <DateInfoHome left="Time" right={'("00:00");'} filled position="third" />
        <DateInfoHome left="Torres del Paime -" right="Región del Magallanes" position="fourth" />
      </div>
    </section>
  </main>
</Layout>

<style>
  .hero {
    width: 100%;
    display: flex;
    align-items: center;

    & .hero-info {
      width: 50%;

      & p {
        max-width: 439px;
      }

      & .info-buttons {
        display: flex;
        align-items: center;
        gap: 1rem;
        padding: 2rem 0;
      }
    }
    & .hero-date {
      width: 50%;
      height: 502px;
      background-image: url(/assets/torres_del_paine.avif);
      background-size: cover;
      background-repeat: no-repeat;
      position: relative;

      &::after {
        content: '';
        position: absolute;
        top: 0;
        background: radial-gradient(circle, transparent 25%, var(--bg-color) 70%);
        width: 600px;
        height: 502px;
      }
    }
  }
</style>
